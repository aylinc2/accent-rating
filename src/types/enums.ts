export enum ExperimentType {
  LINGUISTIC = "linguistic"
}

export enum Language {
  TR = "tr",
  DE = "de",
}

export enum Gender {
  FEMALE = "f",
  MALE = "m",
}

export enum ParticipantGroup {
  STANDARD = "standard"
}

export enum Phase {
  SETUP = "setup"
}
